---
deployment:
  tasks:
    - export DEPLOYPATH=/home/eeusjxob/OtherApp/vut/codedragon.ng/
    - /bin/bash -c "cd $DEPLOYPATH && php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache"
